# Optional shared-secret bearer token. If set, every request to this server
# must include the header:  Authorization: Bearer <this value>
# Strongly recommended once this is deployed with a public URL.
SLEEPER_MCP_TOKEN=change-me-to-a-long-random-string

# Set automatically by most hosting platforms (Railway, Render, etc). Only
# set this yourself if your platform doesn't inject it.
# PORT=8000
